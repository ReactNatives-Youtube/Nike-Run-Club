import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  progressBarContainer: {
    borderRadius: 4,
    width: '80%',
    borderWidth: 2,
  },
  progressBar: {
    borderRadius: 4,
    borderWidth: 2,
  },
});
